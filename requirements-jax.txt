# JAX dependencies for high-performance computing
# Generated from pyproject.toml [project.optional-dependencies.jax]
# Last updated: v0.7.1+ (Python version check, code quality improvements, test fixes)

# Include core dependencies
-r requirements.txt

# JAX for high-performance numerical computations
# Linux systems with NVIDIA GPU will automatically get CUDA 12 support
jax[cuda12]>=0.7.0 ; sys_platform == 'linux'
jax>=0.7.0 ; sys_platform != 'linux'
jaxlib>=0.4.35

# Note: GPU support on Linux
# - Linux systems will automatically install JAX with CUDA 12 support
# - IMPORTANT: Run 'source activate_gpu.sh' before using GPU
# - Ensure you have CUDA 12.x installed and configured
# - For other CUDA versions, manually install:
#   pip install "jax[cuda11_pip]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
#
# For TPU support:
# pip install "jax[tpu]" -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
#
# For CPU-only on Linux (override default):
# pip install --upgrade "jax[cpu]"
#
# See GPU_SETUP.md for detailed GPU setup instructions
