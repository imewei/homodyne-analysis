# Optional dependencies for Homodyne Scattering Analysis Package
# Generated from pyproject.toml [project.optional-dependencies]
# Last updated: v0.7.1+ (Python version check, code quality improvements, conda integration, simplified shell completion, test fixes)

# Include core dependencies
-r requirements.txt

# Data handling
xpcs-viewer>=1.0.4

# Performance optimization (includes JAX for high-performance computing)
numba>=0.61.0,<0.62.0
jax[cuda12]>=0.7.0 ; sys_platform == 'linux'
jax>=0.7.0 ; sys_platform != 'linux'
jaxlib>=0.4.35
psutil>=5.8.0

# MCMC and Bayesian analysis (with GPU acceleration support)
pymc>=5.0.0
arviz>=0.12.0
pytensor>=2.8.0
corner>=2.2.0
numpyro>=0.13.0
# JAX dependencies for NumPyro GPU acceleration are included above
# Note: Run 'source activate_gpu.sh' before using GPU acceleration

# Robust optimization
cvxpy>=1.4.0

# Gurobi optimization solver (requires license)
gurobipy>=11.0.0

# Shell completion and conda integration (cross-platform support)
argcomplete>=3.0.0

# Note: Shell completion features automatic setup:
# - Conda environment integration with automatic post-installation configuration
# - Linux: Full shell completion with aliases and tab completion  
# - macOS: Shell aliases and shortcuts (bash/zsh compatible)
# - Windows: Batch file shortcuts for Command Prompt and PowerShell
# - Command shortcuts (hm, hc, hr, ha, hgm, hga) always available
# - Help functions (homodyne_help, homodyne_gpu_status)
# - GPU status checking in conda environments
